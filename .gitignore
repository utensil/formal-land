.DS_Store
lean4/.ipynb_checkpoints
*.ipynb
.lsp_symlink
**/.lake
tla/bin/
tla/*.old
tla/*.cfg
**/dist/
tla/*.aux
tla/*.dvi
tla/*.log
tla/*.tex
tla/*.pdf
tla/states

lean4-verso-xp/_out
github-pages/
.aider*

# geotopo's JSON inputs are private: a published page is one self-contained HTML
# carrying its own data. Their canonical home is projects/geotopo/data in the
# private vault; build.py finds it, or takes --data / $GEOTOPO_DATA.
geotopo/data/*.json
