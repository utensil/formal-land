# Additional review rubrics

Tau Ceti's live rubric files remain required and primary. These three
cross-angle rows require evidence artifacts; classify source findings under
the owning Tau Ceti rubric.

| rubric id | question |
|---|---|
| `ut-dependency-readiness` | From the supplied authoritative context, name any unstable prerequisite or record that none remains. |
| `ut-consumer-contract` | Before accepting the candidate's framing, derive the expected owner and contract from the roadmap, neighboring APIs, and immediate consumers. Then classify every public declaration, inventory applicable introduction/elimination, forward/inverse, and zero/successor equations, and test advertised normal forms with bare `simp`. |
| `ut-aggregate-reuse` | Group repeated proof shapes and transport, coercion, instance, or anonymous-constructor plumbing across the aggregate; an exact-result search alone is insufficient. Inventory unconsumed public declarations, thin specializations, and direct imports, with a deletion or replacement probe for each material item. |
